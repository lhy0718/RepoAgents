RepoRepublic is installed in this repository.

- Use Codex CLI as the default worker runtime unless the config explicitly switches to the mock backend.
- Treat `.ai-republic/roles/*.md`, `.ai-republic/prompts/*.txt.j2`, `.ai-republic/policies/*.md`, and `WORKFLOW.md` as binding instructions.
- Operate inside RepoRepublic issue workspaces rather than the maintainer's main checkout.
- Human approval is required before merge, even after reviewer approval.
- Escalate or request changes for secrets, CI/CD files, permission-sensitive settings, or large deletions.
- Keep outputs structured and aligned with the active role schema.

Active preset: `{{ preset.name }}`
Preset scope: {{ preset.scope_summary }}
